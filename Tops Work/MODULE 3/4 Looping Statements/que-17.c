// 17. Calculate 5 numbers from user and calculate number of even and odd using of while loop

#include <stdio.h>

int main() {
    int num;

    while(i<=5){
        printf("enter number:");
        scanf("%d",&num);

        a=num;
        i++;
    }
    b=a;
    sum=
}